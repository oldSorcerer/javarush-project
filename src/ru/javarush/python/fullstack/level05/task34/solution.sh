# Запустите контейнер с Nginx, пробросив порт 80 контейнера на порт 8080 хоста,
# но ограничьте доступ только для локального IP 127.0.0.1


# Проверьте, что контейнер запущен и доступен только с локального хоста
docker ps

# Вы можете проверить доступ, отправив HTTP-запрос на локальный хост
# Например, с помощью команды curl
curl http://127.0.0.1:8080
